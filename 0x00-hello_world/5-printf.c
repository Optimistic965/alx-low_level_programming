#include <stdio.h>
/**
 * main - still the main function
 * Return: 0 with no error as return value
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a peice of art,\n");
	return (0);
}
