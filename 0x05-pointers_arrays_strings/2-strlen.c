#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - counts the length of a string
 * ( this function counts the lenght of a string)
 * @s: string to be counted
 * Return: returns length of string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);

return (length);
}
