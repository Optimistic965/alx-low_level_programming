#include <stdio.h>
#include "main.h"

/**
 * reset_to_98 - function to reset variable
 * (reset the value of a pointer to 98)
 * @*n: is a pointer
 * Return: void
 */
void reset_to_98(int *n)
{
	printf("%d", *n=98);
}
