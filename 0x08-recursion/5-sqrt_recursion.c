#include "main.h"
#include <math.h>

/**
 * _sqrt_recursion - gets the square root of a value
 * @n: num to get it's square root
 * Return: an int
 */

int _sqrt_recursion(int n)
{
	int res = sqrt(n);

	return res;
}
